# Docker image definitions

- `local-compose.yml` at `infra/local-compose.yml` runs Postgres (pgvector), Redis, and MinIO.
- `kicad-cli/` — KiCad CLI wrapper image for native `sch erc` / `pcb drc` (sprint days 6–7). See [`kicad-cli/README.md`](kicad-cli/README.md).
